{
  "name": "schoonmaak",
  "version": "2.0.0",
  "private": true,
  "description": "Weekplanner voor huishoudelijke hulp — draait op GitHub Pages met een Gist als opslag",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec test/*.test.js",
    "dev": "node test/dev-server.js"
  }
}
